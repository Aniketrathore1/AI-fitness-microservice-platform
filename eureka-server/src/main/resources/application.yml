server:
  port: 8761

spring:
  application:
    name: eureka-server

eureka:
  instance:
    hostname: localhost
  client:
    register-with-eureka: false   # This server itself should NOT register
    fetch-registry: false          # This server itself should NOT fetch
    service-url:
      defaultZone: http://localhost:8761/eureka/
